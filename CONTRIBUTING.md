# Contributing

Use a virtual environment, install with `python -m pip install -e ".[dev]"`, and run `make lint`, `make typecheck`, and `make test` before opening a pull request.

Keep gameplay changes covered by tests when they touch physics, AI, settings, or state transitions.
